# Manifest schema for archive/raw data pulls (nightly raws, template downloads).
# These entries document expected layout/checksums; use archive_tools to fetch.
spec: 1
default_cache: ${RAW_PARENT_DIR:-$HOME/data/nickel_raw}
nights:
  example_night:
    night: 20210101
    description: Placeholder entry; replace with real runs as needed.
    expected_files: 0
    notes: "Download via obsn-archive-fetch-night --night 20210101"
templates:
  example_field:
    tag: template_2020wnt
    description: "Placeholder for template field downloads"
    expected_files: 0
    notes: "Generated by scripts/pipeline/05_build_template.sh"
