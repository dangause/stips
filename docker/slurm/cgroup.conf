# Slurm cgroup configuration for Docker
#
# cgroup/v2 is not supported by Slurm 22 in containers.
# Disable all cgroup constraints — we use proctrack/linuxproc instead.
CgroupAutomount=no
ConstrainCores=no
ConstrainRAMSpace=no
