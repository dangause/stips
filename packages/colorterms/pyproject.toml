[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "obs-nickel-colorterms"
version = "0.1.0"
description = "Nickel colorterm fitting scripts and documentation."
authors = [{ name = "Dan Gause" }]
requires-python = ">=3.12"
dependencies = []
readme = "README.md"

[tool.setuptools]
packages = []
