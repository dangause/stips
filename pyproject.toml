[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
    # Code quality tools
    "ruff",
    "pyright",
    "pre-commit>=4.3.0",
    # Testing
    "tox>=4",
    "pytest>=7",
]
notebooks = [
    # Jupyter/notebook support for analysis
    "jupyterlab>=4.4.7",
    "notebook>=7.4.5",
]
analysis = [
    # Data analysis libraries
    "pandas>=2.3.2",
    "pyarrow>=21.0.0",
    "fastparquet>=2024.11.0",
    "pyvo>=1.7",
    "astropy",
    "astroquery",
    "boto3",
    "s3fs",
]
rsp = [
    # Rubin Science Platform integration
    "lsst-rsp>=0.10.0",
]

[tool.ruff]
line-length = 110
target-version = "py312"
src = [
    "packages/obs_nickel",
    "packages/data_tools",
    "packages/defects",
    "packages/refcats",
    "packages/testdata",
    "packages/tuning",
    "packages/colorterms",
    "scripts",
]
extend-exclude = [
    # vendor mirrors
    "packages/lick_searchable_archive/**",
    "packages/refcats/upstream/**",
    "packages/testdata/upstream/**",
]
force-exclude = true

[tool.ruff.lint]
ignore = ["N802", "N803", "N806", "N812", "N813", "N815", "N816", "N999"]
select = [
    "E", # pycodestyle
    "F", # pyflakes
    "N", # pep8-naming
    "W", # pycodestyle
]
extend-select = [
    "RUF100", # Warn about unused noqa
]

[tool.ruff.lint.pycodestyle]
max-doc-length = 79

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.pytest.ini_options]
# All tests (including data_tools unit tests) live in obs_nickel/tests/
# Add additional packages here as they add tests/ directories
testpaths = [
    "packages/obs_nickel/tests",
    "packages/data_tools/tests",
    # "packages/defects/tests",
    # "packages/refcats/tests",
    # "packages/colorterms/tests",
    # "packages/tuning/tests",
]
# Exclude vendor packages and upstream directories from test discovery
norecursedirs = [
    ".git",
    ".venv",
    "build",
    "dist",
    "*.egg-info",
    "packages/lick_searchable_archive",
    "packages/refcats/upstream",
    "packages/testdata/upstream",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests (deselect with '-m \"not integration\"')",
]

[tool.pyright]
pythonVersion = "3.12"
venvPath = "."
venv = ".venv"
include = [
    "packages/obs_nickel",
    "packages/data_tools",
    "packages/defects",
    "packages/refcats",
    "packages/testdata",
    "packages/tuning",
    "packages/colorterms",
    "scripts",
]
exclude = [
    ".venv",
    ".uv-cache",
    "logs",
    "**/build",
    "**/dist",
    "packages/lick_searchable_archive",
    "packages/refcats/upstream",
    "packages/testdata/upstream",
]
ignore = [
    # LSST pipeline configs are executed with an injected `config` object.
    "packages/obs_nickel/configs",
]
# Keep noise down when LSST/Rubin dependencies are not in the active venv.
reportMissingImports = "warning"
reportMissingModuleSource = "warning"
reportOptionalMemberAccess = "warning"
reportOptionalOperand = "warning"
reportOptionalCall = "warning"
reportAttributeAccessIssue = "warning"
reportGeneralTypeIssues = "warning"
reportArgumentType = "warning"
reportReturnType = "warning"
reportOperatorIssue = "warning"
reportUnsupportedDunderAll = "warning"
reportCallIssue = "warning"
typeCheckingMode = "basic"
