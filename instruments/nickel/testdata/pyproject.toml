[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "obs-nickel-testdata"
version = "0.1.0"
description = "Tiny, deterministic Nickel fixtures for unit/integration tests."
authors = [{ name = "Dan Gause" }]
requires-python = ">=3.12"
dependencies = []
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
